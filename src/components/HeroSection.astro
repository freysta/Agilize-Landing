<section class="relative min-h-screen flex items-center justify-center overflow-hidden bg-gradient-to-br from-azul-principal via-gray-900 to-azul-principal">
  <!-- Background Pattern -->
  <div class="absolute inset-0 opacity-5">
    <div class="absolute inset-0" style="background-image: radial-gradient(circle at 25% 75%, #FFDE59 2px, transparent 2px), radial-gradient(circle at 75% 25%, #FFFFFF 2px, transparent 2px); background-size: 50px 50px;"></div>
  </div>

  <!-- Floating Elements -->
  <div class="absolute top-20 left-10 w-20 h-20 bg-amarelo rounded-full opacity-10 animate-float"></div>
  <div class="absolute top-40 right-20 w-16 h-16 bg-white rounded-full opacity-5 animate-float stagger-1"></div>
  <div class="absolute bottom-40 left-20 w-24 h-24 bg-amarelo rounded-full opacity-8 animate-float stagger-2"></div>
  <div class="absolute bottom-20 right-10 w-18 h-18 bg-white rounded-full opacity-6 animate-float stagger-3"></div>

  <div class="container mx-auto px-4 text-center relative z-10">
    <!-- Main Headline -->
    <h1 class="text-5xl md:text-7xl font-bold text-white mb-6 animate-fade-in-up leading-tight">
      Transforme seu <span class="gradient-text">Negócio</span> com a <strong>Agilize</strong>
    </h1>

    <!-- Subheadline -->
    <p class="text-xl md:text-2xl text-gray-300 mb-12 max-w-4xl mx-auto leading-relaxed animate-fade-in-up stagger-1">
      A <strong>Agilize</strong> é a escolha certa para empresários que buscam excelência em serviços contábeis, administrativos e tecnológicos.
      <span class="text-amarelo font-semibold">Resultados comprovados, processos ágeis e atendimento personalizado.</span>
    </p>

    <!-- CTA Buttons -->
    <div class="flex flex-col sm:flex-row gap-4 justify-center items-center animate-fade-in-up stagger-2">
      <a href="#contato" class="group bg-gradient-to-r from-amarelo to-yellow-400 text-azul-principal font-bold py-4 px-8 rounded-2xl hover:from-yellow-400 hover:to-yellow-500 transform hover:scale-105 transition-all duration-300 shadow-2xl hover:shadow-yellow-400/25">
        <span class="flex items-center">
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
          </svg>
          Começar com a Agilize Agora
        </span>
      </a>
      <a href="#sobre" class="group border-2 border-white text-white font-bold py-4 px-8 rounded-2xl hover:bg-white hover:text-azul-principal transform hover:scale-105 transition-all duration-300">
        <span class="flex items-center">
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          Saiba Mais
        </span>
      </a>
    </div>

    <!-- Scroll Indicator -->
    <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
      <div class="w-6 h-10 border-2 border-white rounded-full flex justify-center">
        <div class="w-1 h-3 bg-white rounded-full mt-2 animate-pulse"></div>
      </div>
    </div>
  </div>

  <!-- Bottom Wave -->
  <div class="absolute bottom-0 left-0 right-0">
    <svg viewBox="0 0 1440 120" class="w-full h-auto">
      <path fill="#ffffff" d="M0,32L48,37.3C96,43,192,53,288,58.7C384,64,480,64,576,58.7C672,53,768,43,864,48C960,53,1056,75,1152,80C1248,85,1344,75,1392,69.3L1440,64L1440,120L1392,120C1344,120,1248,120,1152,120C1056,120,960,120,864,120C768,120,672,120,576,120C480,120,384,120,288,120C192,120,96,120,48,120L0,120Z"></path>
    </svg>
  </div>
</section>
